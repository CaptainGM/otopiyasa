# OtoPiyasa Mobil

Flutter ile yazılmış Android uygulaması. Web API'sini (`/api/*`) kullanır.

## Çalıştırma

```bash
flutter pub get
flutter run
```

Emülatörde API adresi otomatik `10.0.2.2:3000` olur. Önce web tarafını `npm run dev` ile aç.
